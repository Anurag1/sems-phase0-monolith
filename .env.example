# App
APP_HOST=0.0.0.0
APP_PORT=8080
API_KEY=replace-with-client-api-key

# Postgres
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=sems
POSTGRES_USER=sems_user
POSTGRES_PASSWORD=sems_password

# LLM / Embedding provider
EMBEDDING_URL=https://api.openai.com/v1/embeddings
EMBEDDING_MODEL=text-embedding-3-small
LLM_URL=https://api.openai.com/v1/chat/completions
LLM_MODEL=gpt-4o-mini
PROVIDER_API_KEY=sk-REPLACE_ME

# Memory & binder settings
HOT_MAX_AGE_SECONDS=600
WARM_MAX_AGE_SECONDS=86400
COMPRESSED_DIM=256
BINDER_SCORE_THRESHOLD=0.80
